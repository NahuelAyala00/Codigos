/*
6. Crie uma função para validar uma entrada de dados. A função receberá como 
argumento um caractere, e deverá retornar 0 (zero) se o caractere não for um 
digito decimal (0,1,2...9) ou 1 (um), se o caractere for decimal. Lembrando 
que o código ASCII do 0 (zero) é 48 e do 9 (nove) 57.
*/

#include <stdio.h>
#include <stdlib.h>

int decimal (float n1){
    int aux;

    if(n1 == aux){
        printf("\n0\n");
    }

}


int main (){
    float numero;
    
    printf("Informe um numero:");
    scanf("%f",&numero);
    fflush(stdin);
    


    return 0;
}