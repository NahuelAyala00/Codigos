/*Criar um script de um relógio (Data e hora)

Criar um script que ordene uma lista de nomes

Criar um script para criar uma nova string sem a primeira e última letra de uma string já existente

Exemplo: Javascript → avascrip

Criar um script que recebe o input de uma palavra e mostre a quantidade de letras

Criar um script que recebe o input de um nome e verifica se esse nome está presente na seguinte lista: [“João",”Pedro”,”Alex”], caso esteja retornando true, caso não false.

Exemplo: “Paulo” → false; “Pedro” → true

Criar um script que substitua as letras de determinada palavra

Exemplo: Javascript → Jovoscript (a → o)

Criar um script que faça todas as operações matemáticas

Criar um script com todos os operadores de comparação (igual, diferente, maior e igual …)

Criar um script que retorne a URL da página

Criar um Loop (for) onde uma lista de sites recebe “.com” automaticamente no final.

Exemplo: [“clinicorp',”google”,”facebook”] → “clinicorp.com”, “facebook.com”, “google.com”

Criar um script que faça as partições de uma string: slice, substring e substr

Criar um script que que faça os métodos com um array: push, join, shift, unshift, concat, splice e slice

Criar um script que mostre os dias restantes até o seu aniversário 

E dessa data conseguir tirar separadamente: Ano, mês, dia e hora

Exemplo:  Thu Feb 24 2022 08:41:35 GMT-0300 (Brasilia Standard Time) → 2022, 1, 24, 8

Criar um script que adicione 5 dias a partir de hoje

Depois fazer com um input de um valor. Exemplo: o input recebe o número 2, contando que hoje é dia 23/02/2022, o resultado será 25/02/2022

Criar uma página com uma representação do “antes e depois” de algum procedimento. Precisam ter 3 botões, “Antes” (Aparece só o antes), “Depois” (Aparece só o depois”), “Ambos” (Aparece tudo)

Os botões devem ser componentes

Fazer um semáforo simulando um real em funcionamento

As lâmpadas do semáforo deve ser um componente 

Fazer um TO-DO-LIST ( pode ser uma agenda de paciente ou uma lista de procedimentos)
*/