# A = PI R.R
from math import pi
raio = float(input("Informe o raio do círculo(em cm):"))

AreaDoCirculo = pi * raio * raio

print("Um círculo de raio {} cm tem uma área de {:.2f2} cm".format(raio,AreaDoCirculo))
